// ─────────────────────────────────────────────────────────────────────────────
//  GLOBAL APP CONFIG
//
//  DEMO_MODE = true  →  bypasses Firebase Auth + API with realistic mock data.
//                        Use this while backend / API is not yet connected.
//
//  DEMO_MODE = false →  full production mode (real Firebase auth + real API).
//                        Flip this once the backend team gives the green light.
// ─────────────────────────────────────────────────────────────────────────────

export const DEMO_MODE = true;
